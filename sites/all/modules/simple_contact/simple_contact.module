<?php

function simple_contact_form_alter(&$form, &$form_state, $form_id){
if ($form_id == "contact_site_form") {
    // $subject = $form['subject']['#default_value'] = ;
    // unset($form['subject']);
    $form['subject']['#type'] = 'hidden';
    print '<pre>';
    print_r($form);
    print '<pre>';
}

}
